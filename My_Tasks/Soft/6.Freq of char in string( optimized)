import java.util.Scanner;
import java.util.HashMap;
public class Freq_of_char_Hashamp {
    public static void main(String[] args) {
        Scanner sc= new Scanner(System.in);
        System.out.print("Enter the string: ");
        String str=sc.nextLine();
        //STORE CHAR FREQ
        HashMap <Character, Integer> freqmap= new HashMap<>();
        // count freq of each char
        for(int i=0;i<str.length();i++){
            char ch= str.charAt(i);
            if(freqmap.containsKey(ch)){
                freqmap.put(ch,freqmap.get(ch)+1);
            }else{
                freqmap.put(ch,1);
            }
        }
        for(char key:freqmap.keySet()){
            System.out.println(key+"->"+freqmap.get(key));
        }
    }
}


Enter the string: saravanan
a->4
r->1
s->1
v->1
n->2


Time Complexity: O(n) → we scan the string once and each HashMap operation is O(1) on average.

Space Complexity: O(k) → where k = number of unique possible characters




















